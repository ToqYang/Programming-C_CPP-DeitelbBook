#include <stdio.h>

/**
 * main - Look the Difference between i and d
 *        i = Detect any base
 *        d = Detect integers
 *
 * Return: 0 if is success
 */
int main(void)
{
	int x = 0, y = 0;

	scanf("%i%d", &x, &y);
	printf("%d %d\n", x, y);

	return (0);
}
