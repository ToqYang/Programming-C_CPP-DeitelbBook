#include <stdio.h>
#include <string.h>


/**
 * main - Show initialize array vowel to VOWEL
 *
 * Return: 0 if is success
 */
int main(void)
{
	/* I Can Initialize the Array of this Forms */
	char vowel[] = "AEIOU";
	char vowel[] = {'A', 'E', 'I', 'O', 'U', '\0'};

	return (0);
}
